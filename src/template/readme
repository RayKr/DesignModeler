Template 模板模式

1. 在一个方法中定义一个算法骨架，而将一些步骤延迟到子类中。
2. 模板方法使得子类可以不改变算法骨架的情况下，重新定义算法中的某些步骤。
3. 此模式适用于业务撤销，在父类定义算法骨架，子类可以通过钩子、重载重新定义。

当我创建一个模板方法时，什么时候使用抽象方法，什么时候使用钩子？
答： 当子类必须提供算法中某个方法或步骤的实现时，就使用抽象方法。
    如果算法的这个部分是可选的，就用钩子。

比如在决定是否派发工单时候，可以使用钩子。
